export const config = {
    // Nombre de particules au démarrage de l'appli
    ballCount : 0,
    // Paramètre physique des particules
    minSize: 12,
    maxSize: 22,
    maxSpeed: 3,
    background: "rgba(0,0,0,0.22)",
    // Définir la proportion du tirage automatique
    shapeMix: {
        skull: 0.14,
        rabbit: 0.14,
        squid: 0.14,
        ball: 0.14,
        star: 0.14,
        heart: 0.14,
        diamond: 0.14
    }
}